<%= bootstrap_form_for @form, :url => project_articles_path(@project) do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :description %>

  <%= submit_tag t(:submit), :class => 'btn btn-success' %>
<% end %>