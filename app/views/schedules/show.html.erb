<%= link_to t(:edit), edit_project_schedule_path(@project, @schedule), class: 'btn btn-primary' %>
<%= link_to t(:delete), project_schedule_path(@project, @schedule), :method => 'delete', class: 'btn btn-danger', data: {confirm: t(:delete_message, :scope => :schedule)} %>
