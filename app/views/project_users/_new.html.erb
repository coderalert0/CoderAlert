<%= bootstrap_form_for @form, :url => project_users_path do |f| %>
  <div class="modal-header">
    <h4 class="modal-title" id="myModalLabel">Permission User</h4>
    <button type="button" class="close" data-dismiss="modal">x</button>
  </div>

  <div class="modal-body">
    <%= f.select :user, options_for_select(@users.map { |u| [ u.full_name, u.id ] }), :label_class => 'required' %>
    <%= link_to 'New Account', new_user_invitation_path %>
  </div>

  <div class="modal-footer">
    <%= f.submit_tag t(:submit), :class => 'btn btn-success' %>
  </div>
<% end %>