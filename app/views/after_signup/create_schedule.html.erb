<h2>Create a Schedule</h2>

<div class="alert alert-info">
  A schedule allows you to pre-determine the order of responsibility for production support tickets.
  <br/>For example, if your sprint is bi-weekly you could rotate responsibility to an engineer every sprint
  <br/>by setting the frequency to bi-weekly and entering a sprint start date.
  <br/><br/>If you are unsure about the schedule, you can always edit the schedule after the setup process is complete.
</div>

<%= bootstrap_form_for @form, :url => wizard_path, :method => :put do |f| %>
  <%= f.text_field :name, :label_class => 'required' %>
  <%= f.select :frequency, options_for_select(Schedule.frequencies.keys.map { |s| [ t(s, :scope => :frequency), s ] }), :label_class => 'required' %>
  <%= f.datetime_select :start, :order => [:hour, :minute, :day, :month, :year], :label_class => 'required' %>
  <%= f.datetime_select :end, :order => [:hour, :minute, :day, :month, :year] %>

  <% @current_project.users.confirmed.each do |user| %>
    <%= label_tag "#{user.first_name}" %>
    <%= text_field_tag "create_schedule_form[users][#{user.id}]" %><br/>
  <% end %>

  <%= submit_tag t(:submit), :class => 'btn btn-success' %>

  <%= link_to 'skip', previous_wizard_path %>

<% end %>